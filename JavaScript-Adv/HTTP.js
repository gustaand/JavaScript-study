// ---- PETICIÓN HTTP ----




/* Es una petición que envia el programador al servidor, y el servidor devuelve una información.
relación entre client & server:
    cliente = nuestro navegador, la página que navega el usuario (frontEnd)
    servidor = guarda información, tiene la página web completa (backEnd)

Las peticiones http no guardan información.

antes de trabajar con esto tenemos que ver como funcionan los datos JSON (datos estructurados).
*/